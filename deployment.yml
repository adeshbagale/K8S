apiVersion: app/v1
kind: Deployment
metadata: 
    name: nginx-deployment
spec:
    replicas: 10
    template:
        metadata:
            labels:
                app: my-nginx
        spec:
            containers:
              - name: nginx
                image: nginx:1.22.1
                ports:
                  - containerPort: 80
